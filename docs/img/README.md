# Landing-page images

- `poster.png` (optional) — a thumbnail shown on the demo videos before they play.
  Use a nice frame from one of your clips. If absent, the video just starts black.

The main visuals on the page are the demo videos (see `docs/media/`) and a built-in
CSS mockup of the overlay, so no screenshots are strictly required.
